[tool.ruff]
line-length = 119
exclude = [
  "__pycache__",
  "usbsdmux.egg-info",
  ".pybuild",
  "build",
  "debian",
  "env",
  "venv",
  "envs",
  "dist",
]

[tool.ruff.lint]
select = ["B", "E", "F", "I", "SIM"]
